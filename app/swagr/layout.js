export const metadata = {
  title: 'SWAGR AI — Promo discovery experience',
  description: 'Build a reasoned promotional-product direction from your audience, moment, quantity, budget, timing, and preferences.',
};

export default function SwagrLayout({ children }) {
  return children;
}
